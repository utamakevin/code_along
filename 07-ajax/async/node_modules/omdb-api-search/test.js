const omdbApiWrapper = require('./index')

const omdb = omdbApiWrapper.createClient('2f6435d9')

omdb.findOneByTitle('jaws', (err, movie) => {
  console.log(movie);
})

// omdb.findManyByTitle('jaws').then(res => {
//   console.log(res.Search.length)
// })